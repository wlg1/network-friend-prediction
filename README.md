# network-friend-prediction

To run the scripts in /uncorrupted, use:
  python run_all.py
  
To run the scripts in /uncorrupted, use:
  run_mult_rand.py
  
To run grid search in /gridsearch, use:
  gridsearch.py
